import { Quiz as QuizComponent } from "@/components";

const Quiz = () => {
  return (
    <div className="flex items-center min-h-screen">
      <QuizComponent />
      </div>
  );
};

export default Quiz;
